# Disabled since it requires udev support which doesn't work out of the box on talosctl cluster
#
# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: openebs-dynamic-localpv-provisioner
#   namespace: argocd
# spec:
#   destination:
#     server: https://kubernetes.default.svc
#   project: platform
#   source:
#     path: platform/openebs-dynamic-localpv-provisioner
#     repoURL: https://github.com/dbackeus/activedeployment.git
#   syncPolicy:
#     automated:
#       allowEmpty: true
#       prune: true
